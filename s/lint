#!/usr/bin/env bash
set -euo pipefail

# Check formatting and run static analysis, matching CI.
cd "$(git rev-parse --show-toplevel)"

dart format --output=none --set-exit-if-changed .
exec flutter analyze "$@"
