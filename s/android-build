#!/usr/bin/env bash
set -euo pipefail

# Build a release APK for the prod flavor.
cd "$(git rev-parse --show-toplevel)"

exec flutter build apk --release --flavor prod --target lib/main_prod.dart "$@"
